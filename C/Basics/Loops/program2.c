#include <stdio.h>
 
int main()
{
    int n = 1; // initialize starting value

    while (n <= 10) // write a while loop inside condition
    {
        printf("%d\n", n); 
        n++; // updates the value 
    }
    
    return 0;
}