#include <stdio.h>

int main()
{
    int number = 10;

    // if first statement is true then the program stops executing (if all the statements is true)
    // if (number < 30)
    // {
    //     printf("%d is less than 30\n", number);
    // }
    // else if (number < 50)
    // {
    //     printf("%d is less than 50\n", number);
    // }

    if (number < 30)
    {
        printf("%d is less than 30\n", number);
    }
    if (number < 50)
    {
        printf("%d is less than 50\n", number);
    }
    
    return 0;
}